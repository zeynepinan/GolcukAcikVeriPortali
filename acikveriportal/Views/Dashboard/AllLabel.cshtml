
@{
    ViewBag.Title = "AllLabel";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@using acikveriportal.Models
@model List<Label>

<body>
    <input type="button" class="btn btn-success" value="Etiket Ekle" onclick="location.href='@Url.Action("CreateLabel", "Dashboard")'" />
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    @if (Model.Count() > 0)
                    {
                        <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Ad a  göre ara.." title="Type in a name">
                        <table class="table table-hover" id="list">
                            <thead>
                            <th>Ad</th>
                            <th>Düzenle</th>
                            <th>Sil</th>
                            </thead>
                            <tbody>
                                @foreach (Label label in Model)
                                {
                                    <tr>
                                        <td>@label.Name</td>
                                        <td>@Html.ActionLink("Düzenle", "EditLabel", "Dashboard", new { @id = $"{label.Id}" }, null)</td>
                                        <td>@Html.ActionLink("Sil", "DeleteLabel", "Dashboard", new { @id = $"{label.Id}" }, null)</td>
                                       
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="text-center alert-info">
                            <h3> Etiket Yok</h3>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
</body>



