
@{
    ViewBag.Title = "AllLicense";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



@using acikveriportal.Models
@model List<License>

<body>
    <input type="button" class="btn btn-success" value="Lisans Ekle" onclick="location.href='@Url.Action("CreateLicense", "Dashboard")'" />
    <section>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    @if (Model.Count() > 0)
                    {
                        <input type="text" id="myInput" onkeyup="myFunction()" placeholder="Ad a  göre ara.." title="Type in a name">
                        <table class="table table-hover" id="list">
                            <thead>
                            <th>Ad</th>
                            <th>Düzenle</th>
                            <th>Sil</th>
                            </thead>
                            <tbody>
                                @foreach (License license in Model)
                                {
                                    <tr>
                                        <td>@license.Name</td>
                                        <td>@Html.ActionLink("Düzenle", "EditLicense", "Dashboard", new { @id = $"{license.Id}" }, null)</td>
                                        <td>@Html.ActionLink("Sil", "DeleteLicense", "Dashboard", new { @id = $"{license.Id}" }, null)</td>
                                   
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="text-center alert-info">
                            <h3> Lisans Yok</h3>
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>
</body>



