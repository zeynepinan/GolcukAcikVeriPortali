@using acikveriportal.Models
@model Organization
@{
    ViewBag.Title = "DeleteOrganization";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="text-center">
    <h2 class="text-danger">Silinmek istenen kayıt: @Model.Name</h2>
    <h3>Bu kaydı silmek istediğinize emin misiniz ?</h3>
</div>

<input type="button" class="btn btn-info" value="İptal" onclick="location.href='@Url.Action("AllOrganization", "Dashboard")'" />
<input type="button" class="btn btn-danger" value="Sil" onclick="location.href='@Url.Action("OrganizationDeleted", "Dashboard", new { @id = $"{Model.Id}" }, null)'" />
